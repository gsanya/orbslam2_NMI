#version 330 core

// Interpolated values from the vertex shaders
in vec3 fragmentColor;

// Ouput data
out vec3 color;

void main()
{
	//color = vec3(1,1,1);// Output color = white 
	color = fragmentColor;
}